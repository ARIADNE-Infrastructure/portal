export interface MainMenuItem { // A bit verbose with props - because of css purify
  path: string,
  name: string,
  icon: string,
  color: string,
  bg: string,
  border: string,
  hover: string,
}
